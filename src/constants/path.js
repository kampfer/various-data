import path from 'path';

export const ROOT_PATH = path.resolve();
export const DATA_STORE_PATH = path.resolve(ROOT_PATH, 'data');
export const DRIVER_PATH = path.resolve(ROOT_PATH, 'bin/chromedriver.exe');